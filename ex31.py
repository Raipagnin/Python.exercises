#x = float(input('Qual a distancia em KM da sua viagem?: '))
#price = x * 0.50 if x <= 200 else price = x * 0.45
#print('O preco da sua viagem eh {}reais'.format(price)) NAO FUNCIONAAAAAA

# pergunte a distancia de uma viagem em km e calcule: 0,50 para cada km se a viagem for ate 200km e 0,45 pra alem disso

# if x <= 200:
#   print('O valor da sua passagem eh de {:.2f} reais'.format(x * 0.50))
# else:
#  print('O valor da sua passagem eh de {:.2f} reais'.format(x * 0.45))


# if x <= 200:
#   price = x * 0.50
# else:
#   price = x * 0.45
